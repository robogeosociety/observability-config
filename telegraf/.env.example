# Telegraf secrets — copy to telegraf/.env (gitignored, chmod 600).
# deploy.sh sources this and bakes INFLUX_TOKEN into the deployed config.

# Write-only token scoped to the `system` bucket
# (influx auth create --org home --write-bucket <system-bucket-id>).
INFLUX_TOKEN=
