# discord-claude-heatmap secrets — copy to .env (gitignored, chmod 600).
INFLUX_READ_TOKEN=       # read-all token (ask-dash/.env)
DISCORD_BOT_TOKEN=       # OpsBot bot token (discord-ops/.env — NOTE: ask-dash/.env's is empty)
DISCORD_CHANNEL_ID=1520850187105341710
INTERVAL=60
