# Copy to .env (chmod 600) and fill. Gitignored.
# Needs an InfluxDB token that can READ the source buckets (system, transit_tracker,
# tempest_archive, …) and WRITE the ops bucket.
INFLUX_URL=http://localhost:8086
INFLUX_ORG=home
INFLUX_TOKEN=
# optional overrides:
# ANOMALY_BUCKET=ops
