[project]
name = "anomaly-detector"
version = "0.1.0"
description = "Window z-score anomaly detector → ops.anomaly event stream"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6"]

[tool.uv]
package = false
