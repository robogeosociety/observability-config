services:
  influxdb:
    image: influxdb:2.7-alpine
    container_name: influxdb
    restart: unless-stopped
    # Resource caps = 50% of the OrbStack VM (4 CPU / 4096 MiB). Limits blast
    # radius: a runaway container is OOM-killed by Docker, not allowed to thrash
    # the whole VM into host swap (see the 2026-05-31 watchdog panic).
    mem_limit: 2048m
    cpus: 2.0
    pids_limit: 512
    ports:
      - "8086:8086"
    volumes:
      # Data + config bind-mounted to the 2 TB external /Volumes/dev disk: TSM
      # time-series data grows large and belongs on the big disk, not the small
      # internal one. TRADE-OFF: influxdb now REQUIRES /Volumes/dev mounted BEFORE
      # OrbStack starts it — otherwise INIT_MODE:setup re-inits a fresh empty DB on
      # an empty placeholder path. The boot-time /dev mount must be guaranteed.
      - /Volumes/dev/observability/influxdb/data:/var/lib/influxdb2
      - /Volumes/dev/observability/influxdb/config:/etc/influxdb2
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: ${INFLUX_ADMIN_USER}
      DOCKER_INFLUXDB_INIT_PASSWORD: ${INFLUX_ADMIN_PASSWORD}
      DOCKER_INFLUXDB_INIT_ORG: ${INFLUX_ORG}
      DOCKER_INFLUXDB_INIT_BUCKET: ${INFLUX_BUCKET}
      DOCKER_INFLUXDB_INIT_RETENTION: 0
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: ${INFLUX_ADMIN_TOKEN}
    healthcheck:
      test: ["CMD", "influx", "ping"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
